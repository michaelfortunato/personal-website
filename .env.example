# Example env file (do not put secrets in git).
# Copy to `.env.local` (ignored) if you prefer dotenv-style.

# Only required if you install private `@gsap/*` packages from GreenSock.
GSAP_AUTH_TOKEN=

# Optional: bundle analyzer (see `next.config.js`)
ANALYZE=false

